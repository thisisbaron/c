#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
  // 请在此输入您的代码
  if(1){
    printf("yes");
  }else{
    printf("no");
  }
  return 0;
}
//Z→Q 表示如果张老师来了，问题可以得到解答。L→Q表示如果李老师来了，问题可以得到解答。结论 (Z∨L)→Q(Z∨L)→Q 表示如果张老师或李老师来了，问题可以得到解答。
